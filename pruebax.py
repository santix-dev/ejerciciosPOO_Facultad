# class Vector(object):
# 	"""docstring for vector"""
# 	def __init__(self, x,y):
# 		self.__y = y
# 		self.__x = x
# 	def __add__(self,otro):
# 		self.__x+=otro.__x
# 		self.__y+=otro.__y
# 		return self.__x
# 	def __str__(self):
# 		return f"{self.__x}{self.__y}"
# 	def __mul__(self,escalar):
# 		self.__x*=5
# 		self.__y*=5


# vector=Vector(5,3)
# vector*5
# print(vector)
# vector2=Vector(10,1)
# vector+vector2
# print(vector)
class Nodo(object):
	"""docstring for Nodo"""
	def __init__(self,pene):
		self.__pene = pene
		self.__sig=None
	def agregar(self):
		
class pito(object):
	"""docstring for pito"""
	def __init__(self,largo,grosor,color):
		self.__largo = largo
		self.__grosor = grosor
		self.__color = color

class lista(object):
	"""docstring for lista"""
	self.__cabeza=dirNodo
	self.cola.sig
	def __init__(self):
		self.__cabeza = nuevopene
	def agregar(self):
		largo,ancho,color=eval(input("Ingrese datos: "))
		nuevopene=pito(largo,ancho,color)
		self.cola.sig=nuevopene.sig=self.__cabeza
		self.cola=
	def agregar(self):
		aux=self.cabeza
		while aux.sig!=None:
			aux=aux.sig
struct nodo{
	reg persona
	*nodo sig
}
1

		
		